# 2026-09-10

## Wolfcha「只配一个 LLM 就能跑」改造(OpenAI 兼容网关)

需求:只配 baseUrl/key/thinking/思考深度 即可本地运行,免 Supabase/TTS/支付。
用户选择:标准 OpenAI 兼容协议 + 前端 UI 填写。已直接改代码。

### 改动清单
- `src/lib/api-keys.ts`:新增 `wolfcha_openai_base_url/api_key/model/thinking_enabled/reasoning_effort` storage 存取层;`isOpenAICompatConfigured()` 判定;`hasLocalLlmKey`/`clearApiKeys` 纳入;`validateApiKeyBalance` 对 openai 直接 valid。
- `src/types/game.ts`:新增 `ModelProvider` 联合类型(加 `"openai"`)。
- `src/lib/llm.ts`:Provider 加 openai;`buildModelSourceHeaders` 带 `X-OpenAI-Api-Key/X-OpenAI-Base-Url`;新增 `applyOpenAICompatOverride`(读取用户模型/思考/深度),generateCompletion/Batch/Stream 三入口开头强制收口同一模型+provider=openai;generateJSON 在 openai 配置下不再强制 json_object。
- `src/app/api/chat/route.ts`:Provider 加 openai;读 x-openai-* 头;batch(runBatchItem)与 single POST 均新增 openai 转发分支(拼 /chat/completions、Bearer、SSE 透传、thinking 开→发 `reasoning_effort`);hasCustomKeys 纳入 openai 头。
- `src/lib/server-game-observability.ts` + `src/types/database.ts`:`provider`/`p_provider` union 补 "openai"。
- `src/lib/supabase.ts`:缺 env 不再模块加载即抛,改导出未登录 stub(getSession/getUser→null,其余 auth 返回错误)。
- `src/lib/supabase-admin.ts`:缺 env 时导出 null 强转 client + `ensureAdminClient()` 抛错(不再 import 时炸)。
- `src/lib/api-auth.ts`:`LLM_ONLY_MODE=true` 时 authenticateRequest 直接放行 local user。
- `src/lib/demo-config-server.ts`:`LLM_ONLY_MODE=true` 时 demo 视为 active → 前端免登录/免积分/免对局会话。
- `src/components/game/UserProfileModal.tsx`:custom tab 顶部新增「OpenAI 兼容网关」设置(地址/Key/模型名/思考开关/思考深度 minimal-low-medium-high);openai 三项填齐后隐藏旧三服务商与模型下拉,保存优先走 openai。
- `src/components/game/WelcomeScreen.tsx`:`hasActiveExternalModelSource` 纳入 `isOpenAICompatConfigured`。
- `src/i18n/messages/zh.json`/`en.json`:新增 `customKey.openai.*`。
- `.env.example`:新增 `LLM_ONLY_MODE` 说明(仅本地/内网开启,会绕过登录校验)。

### 使用方式
`.env.local` 加 `LLM_ONLY_MODE=true`(其余 env 可全空);浏览器打开后在「设置→用我自己的 key 玩→OpenAI 兼容网关」填 API 地址/Key/模型名 + 思考开关/深度并保存。角色生成/发言/夜间总结/复盘全走该模型。

### 验证
- `pnpm exec tsc --noEmit` 通过;eslint 改动文件 0 error;`pnpm build` 后台验证中。
- 思考参数:标准 OpenAI 兼容 → thinking 开启且设深度时请求体带 `reasoning_effort`;关闭时不发。部分网关(火山/Kimi thinking 协议)未适配,后续如需要按 `thinking:{type,budget_tokens}` 扩展。
- 注意:openai 分支在 runBatchItem 内的插入位置容易看花,改前先 Read 确认大括号。

### 追加:构建验证限制
- `pnpm build` 在本 CLI 沙箱中失败:webpack 写 `.next` 产物目录时沙箱 fs broker(node-brokered-fs-shim)并发 mkdir 报 ENOENT/EEXIST,非代码错误(tsc/eslint 均过)。需用户在本地终端跑 `pnpm build`/`pnpm dev` 验证。
- 曾 `mv .next .next.old_20260910`(内含旧缓存,user 可自行删除);后一次 build 又生成了部分 `.next`。

### 追加:设置入口不可见修复
现象:LLM_ONLY/游客(未登录)下 header 没有账户按钮(只有「登录」),「用我自己的 key 玩 → OpenAI 网关」输入区藏在开关展开区后,用户找不到。
修复:
- `WelcomeScreen.tsx`:未登录时 header(desktop)新增「配置 LLM 网关」按钮、移动菜单新增同款条目 → `setUserProfileDefaultTab("custom")` + 打开 Profile 弹窗。
### 追加:修复 "Unknown model: deepseek-v4-flash"
根因:chat route 的 **single(单请求)路径** modelProvider 判定白名单漏加 `"openai"`(batch 路径加了,single 漏了)→ provider=openai 被当未知模型 400。
修复(`src/app/api/chat/route.ts`):
- batch(runBatchItem)与 single 两处判定统一:显式 provider → 注册表 → **带 X-OpenAI-Api-Key+Base-Url 即视为 openai**(模型名不受内置注册表限制)。
- LLM_ONLY 下若仍无 provider/key,错误提示改为「请在配置 LLM 网关中填写 API 地址/Key/模型名称」。
tsc/eslint 通过。

### 追加:response_format 分级 + 角色生成 invalid JSON
用户模型(deepseek-v4-pro)自测支持 `json_object`;早前 400 "response_format unavailable" 来自 **json_schema**(character/game-master 大量用 json_schema)。
- 之前误判一刀切禁掉 response_format → 模型无强约束 → "Character batch 0 returned invalid JSON"。
- 修复(chat route openai 分支,batch+single):`json_object` 原样透传;`json_schema` 降级为 `json_object`(字段结构在 prompt 中已含,character-generator 的 buildXxxResponseFormat schema 会作为文本注入 prompt)。
- 另加固 `llm-json.ts` extractFirstJsonCandidate 为字符串感知+括号深度平衡提取(兼容模型夹杂思考/前后缀文本)。
验证:tsc/eslint/单测 structured-output 全过;手工杂文冒烟 OK。

### 追加:json_object 发送做成可配置开关(默认开)
用户诉求:模型可能不支持 json_object,想自己关。
- api-keys:`wolfcha_openai_json_object_enabled`,未显式配置默认 true;set false 写 "false";clear 移除。
- llm.ts:openai 配置时带 `X-OpenAI-Json-Object: true|false` 头。
- chat route openai 分支(batch+single):header=="false" 则完全不发 response_format;否则沿用 json_object 直传 / json_schema→json_object 分级。
- UserProfileModal:openai 设置区新增「启用 json_object 输出」Switch(默认开);handleSave 保存;zh/en 文案 `customKey.openai.jsonObject*`。
tsc/eslint 通过。

### 追加:解析修复"键开头引号丢失"
真实坏样本:`...,\"gender\":\"male\",age\":29,...`(对象键开头 `"` 缺失)。ai-json-fixer aggressive 未修好 → parseJsonTolerant 抛 "Failed to parse JSON response"。
修复 `llm-json.ts`:新增 `fixMissingKeyQuotes`(正则 `([{,]\s*)([A-Za-z_$][\w$-]*)(?="\s*:)`, → 补 `"`),作为候选加入 parseLLMJson。验证:真实样本 count=9 OK、structured-output 单测通过。

### 追加:角色批量失败改"单发补生成"兜底
现象:character batch N(整段解析后仍缺人)直接 throw → 整局重开。
修复 `character-generator.ts` generatePersonaBatch:
1. 整段 parse 失败不再 throw;
2. 缺的角色逐个 `buildFullPersonasPrompt` + `generateJSON`(单人 response_format)补生成,成功即 emit;单人仍失败才 throw(带具体角色名)。
tsc/eslint/单测通过。

### 追加:角色失败弹窗「重试该批」(用户确认交互)
需求:批失败不要整局作废,弹窗让用户选择重试该批。
- `character-generator.ts`:`generateCharacters` options 加 `onBatchRetry({batchStartIndex,attempt,reason})→Promise<boolean>`;批次改为「并发跑→收集失败批→onBatchRetry 确认→只重跑失败批」循环;未提供 onBatchRetry 保持原抛错。
- `useGameLogic.ts`:新增 characterRetry state(挂起)+ characterRetryResolveRef + requestCharacterBatchRetry/confirmCharacterBatchRetry;startGame 普通分支 generateCharacters 传 onBatchRetry;startGame deps 加 requestCharacterBatchRetry(react-compiler lint 要求)。return 暴露 characterRetry/confirmCharacterBatchRetry。
- 新组件 `src/components/game/CharacterRetryDialog.tsx`(标题/批次/尝试次数+原因,取消=放弃开局,重试=重跑该批)。
- `page.tsx`:import + 解构 + 根 div 渲染 `<CharacterRetryDialog />`。
- i18n zh/en `gameLogicMessages.characterRetry.*`。
取消→走原 catch(标记失败、回欢迎页);重试→只重跑失败批,已有成功角色保留。tsc/eslint/2 单测过。

### 追加:response_format 客户端也降级(所见即所得)
用户疑问:前端 /api/chat 请求体仍是 json_schema(此前只服务端转发时降级)。
`llm.ts` applyOpenAICompatOverride 内:openai 模式且 json_object 开关开 → json_schema 就地转 json_object;开关关 → response_format 置 undefined。服务端分级逻辑保留作兜底(老客户端/直连)。tsc/eslint 过。

### 决策:保持薄 node 代理,不做纯前端
用户部署目标 = 本地/内网跑;网关 = 官方 API。结论:保持 LLM_ONLY 薄代理架构(理由:官方 API CORS 未必全放、key 暴露、流式/兼容处理现成、纯前端仍要 Next 编译)。
增强:WelcomeScreen 右上角「配置 LLM 网关」按钮配置后显示 `LLM: {model}`(新 i18n customKey.openai.connected,desktop+mobile 两处,import getOpenAIModel)。tsc/eslint 过。

### 决策变更:用户要打包安卓 APK → 加浏览器直连(Phase 1 完成)
用户真实目标:Capacitor 打包 APK,必须去掉服务端依赖;API Key 存浏览器可接受;网关=官方 API(自述允许 CORS)。
Phase 1(已做,tsc/eslint 过):
- api-keys: `wolfcha_openai_direct`(direct flag)+ `isOpenAIDirectActive()`(configured && flag)。
- 新增 `src/lib/llm-direct.ts`:浏览器直连 OpenAI 兼容网关(completion/batch/stream 三实现,flatten messages、thinking→reasoning_effort、response_format 透传(apply 已预分级)、gameStats addAiCall、SSE 解析、错误 message 提取)。
- `llm.ts` generateCompletion/Batch/Stream 三入口:`isOpenAIDirectActive()` 时直接分流到 llm-direct(在 applyOpenAICompatOverride 之后)。
- UI:i18n + UserProfileModal 新增「浏览器直连」Switch;WelcomeScreen demo 判定合并 `isOpenAIDirectActive()`(免登录免积分)。
Phase 2(待做,需本地 build 验证——沙箱无法 next build):
- 静态导出:next.config output export + 构建脚本把 src/app/api 移出再还原;
- Capacitor 集成与页面/资源路径适配;TTS(浏览器 Web Speech 或关闭);/api/* 404 处兜底确认。

### 追加:直连语义翻转为默认(用户:纯前端,不要开关)
用户要求配好即直连、无后端。改动:
- `isOpenAIDirectActive()` = configured && !proxyFlag;存储 key `wolfcha_openai_direct` 语义反转为"是否经本服务代理"(true=代理)。
- api-keys 导出改为 get/setOpenAIProxyEnabled;UI Switch 改「经本服务代理转发」(默认关=直连);文案/keys `customKey.openai.proxy/proxyDesc`(zh/en)。
- WelcomeScreen demo 判定已并入 isOpenAIDirectActive。
tsc/eslint 通过,无旧 API 残留引用。

### 追加:头像改为本地默认(砍掉 DiceBear 外网)
用户原话:"别外链了 反正访问不了,我作为玩家直接从默认头像里随机挑一个"。
根因:`src/lib/avatar-config.ts` 旧 `buildAvatarUrl` 返回 `https://api.dicebear.com/...`(中国大陆常连不上),且无 onError 兜底;开原神/立绘模式时 AI 有本地立绘,只有玩家头像永远外链 → 表现"只有我作为玩家看不到"。
落地:
- 新增 `scripts/generate-avatars.mjs`(DiceBear notionists v7 在临时目录 `@dicebear/core@7.0.0 + @dicebear/notionists@7.0.0` 跑,`OUT_DIR=public/avatars`)生成 16 张 × (bg 底色 + clear 透明) × (idle/t1/t2 三嘴型)=96 个 SVG,共 1.4MB;嘴型 variant 与原 `TALKING_LIPS` 顺序一致(`variant04→t1`、`variant11→t2`、`variant03→idle`),所以 `Avatar/TalkingAvatar` 组件零改动,说话动画无缝。
- `src/lib/avatar-config.ts` `buildAvatarUrl` 改返回 `/avatars/{bg|clear}/avatar-{NN}-{idle|t1|t2}.svg`;`backgroundColor==="transparent"` → clear(大立绘/说话头像),其余 → bg(普通圆头像)。`AvatarUrlOptions` 接口与 `getHairForSeed/getIdleLipsForSeed/getTalkingLips/getAvatarBgColor/AvatarConfig` 旧 API 全部保留导出以兼容调用方。
- `LandingContent.tsx` 引用的 `AvatarConfig.ALL_HAIR_VARIANTS/IDLE_LIPS/AVATAR_BG_COLORS.length` 数字不变(展示文案无感)。
验证:tsc 0 错;`pnpm build` 成功(`out/avatars/{bg,clear}/...` 共 96 个文件);视觉抽查 16 张 idle/bg + transparent idle/dark + avatar-00 三嘴型,人物/嘴型/底色正常。
- 玩家"随机挑":hash(playerId) % 16 → 每局随机一张本地头像(玩家 playerId 每局新 uuid);同局稳定。
- 没动 package.json 依赖(脚本可重复生成,临时 npm install 用,不污染 devDependencies);脚本头部注释写明重新生成步骤。
- `scripts/generate-avatars.mjs` 注意:`backgroundColor` 必须传数组 (`['e8d5c4']`)、风格 style 模块用 `import * as notionists` 形式;schema 选项值多用数组(候选池),prng 从中随机选。

### 追加:AI 行动失败/超时统一「重试 / 跳过」改造
用户要求:① 60s 发言超时也弹重试;② 投票/女巫用药等全部 AI 决定环节失败都支持重试(不再静默兜底);③ 跳过=沿用各环节现兜底。交互:sonner 常驻 toast 双按钮。
- 新 `src/lib/ai-retry.tsx`:模块级 `askRetryOrSkip({label,description})→Promise<"retry"|"skip">`(toast.custom 自绘卡片,主题变量,getLocale 双语按钮);`runAiTaskWithRetry({task,label,stillValid,onSkip})` 通用重试循环;`cancelPendingAiRetry()` 供游戏重置清理(useGameLogic restartGame/startGame 已调)。
- game-master.ts L0 语义变更:generateAIVote/generateAIBadgeSignupBatch/generateAIBadgeVote/generateBadgeTransfer/generateSeerAction/generateWolfAction/generateWitchAction/generateGuardAction/generateHunterShoot/generateWhiteWolfKingBoomDecision 网络/API 失败由「静默返回兜底值」改为**日志后 throw**(合法 pass/无目标分支仍正常返回;解析失败仍 parsed:null→旧兜底,不进 UI)。调用方全部改 runAiTaskWithRetry,skip=原兜底值(发言=dayPhase.timeout 文案、女巫=pass、守卫/预/狼=undefined、票=-1、警徽移交=-1撕徽、上警={}、猎人/白狼王=null)。
- 白天发言 useDayPhase.runAISpeech:60s 超时不再自动收尾;与 catch 合并进 handleFailure(kind: timeout|error) → 询问;skip=解除 failedRequestRef 阻塞+无已显示段时补 dayPhase.timeout 段+finalize 自然续接;retry=复位后同轮重跑。删了原 catch 的 setDialogue(host,interrupted)+toast 单按钮。
- NightPhase 守卫/狼(原 try/catch 删)/女巫/预、VotePhase.continueVoting、useBadgePhase 上警/警徽票/移交(补 try/finally 防 610 行卡死)、useSpecialEvents 猎人(补 finally)、useGameLogic wwkBoomCheckRef 全部接入。
- i18n zh/en 新增顶层 aiRetry.{guard,wolf,witch,seer,dayVote,badgeSignup,badgeVote,badgeTransfer,hunter,wwkBoom,speech}。
- 测试:useDayPhase.test.ts harness 增加 "@/lib/ai-retry" mock(retryAsks deferred),改写超时/失败用例(超时先询问;skip=timeout 兜底;retry 同轮重跑);vote-resume.test.ts 与 useBadgePhase.test.ts 的 VM modules 补 runAiTaskWithRetry passthrough mock。
- 验证:tsc 0 错;单人上下文回归 116/116 过;lint 改动文件无新 error(仅既有 warnings)。
- 坑:ai-retry.tsx 必须 "use client";VM harness 测试(useDayPhase/vote-resume/useBadgePhase)requireMock 或 modules map 必须加 "@/lib/ai-retry",否则抛 "未配置依赖"。toast.custom 挂起询问期间 isWaitingForAI 保持 true 合理。

### 追加:角色生成偶发 Failed to parse JSON → 根因是 thinking 模型吃掉 max_tokens
用户提供完整响应佐证:deepseek-v4-flash `content:""`、`reasoning_tokens=3050`=base 生成 max_tokens(7×350+600=3050)、`finish_reason:"length"` → content 空 → parseJsonTolerant 必然失败。偶发因为思考长度不定。
修复:
1) character-generator.ts:base 默认预算 `Math.max(2400, count*350+600)` → `Math.max(6000, count*450+1800)`;CHARACTER_PERSONA_BATCH_MAX_TOKENS 4200→7000(单发补生成复用该常量同样受益)。
2) api-keys.ts:新增 `wolfcha_openai_max_tokens` 存取 `getOpenAIMaxTokens():number|null`/`setOpenAIMaxTokens(str)`(空/非法=清除);clearApiKeys 一并移除。
3) llm.ts `applyOpenAICompatOverride`:配置了全局 max tokens 则强制覆盖每个请求 `out.max_tokens`(单点,直连/代理同吃)。
4) ConnectionSettings.tsx openai 区新增「最大输出 Tokens」数字输入(留空=默认);zh/en `customKey.openai.maxTokens*`。
5) base 生成失败改走 onBatchRetry(CharacterRetryDialog 重试该批/取消开局),与 persona 批一致——补上用户“为什么没有重试弹窗”。
验证:tsc 0 错;单人上下文 116/116;lint 改动文件无新 error。
- 备注:model 输出上限若 < 新默认(如 4096)会 400,此类模型应在 UI 把 maxTokens 调小或关闭 thinking。

### 追加:max tokens 默认值展示 + 常量单一事实源
- ConnectionSettings 的「最大输出 Tokens」placeholder/desc 文案补充默认值(zh/en):基础档案公式 max(6000, 人数×450+1800)(7人≈6000/10人≈6300)、人设批次 7000、其余不设上限;填值全局覆盖。
- 新增 `src/lib/character-gen-defaults.ts`(CHARACTER_PERSONA_BATCH_MAX_TOKENS=7000、CHARACTER_BASE_MAX_TOKENS_FLOOR=6000、PER_PLAYER=450、EXTRA=1800、getCharacterBaseProfileMaxTokens(count));character-generator 改从这里取,避免未来漂移;i18n 文案注释指向该文件需同步。

### 追加:DeepSeek 兼容网关要求 json_object prompt 含 "json" 字面
报错 "Prompt must contain the word 'json' in some form to use 'response_format' of type 'json_object'"——DeepSeek-via-OpenAI-兼容网关强校验。触发场景:game-master.generateAIBadgeSignupBatch(走 generateCompletionBatch 而非 generateJSON,不挂 "Respond with valid JSON only" suffix);其它 generateCompletion+json_object 也可能踩。
修复:llm-direct.ts 新增 ensureJsonHintForJsonObject(options),仅当 response_format.type==='json_object' 且 messages 中无 "json"(case-insensitive) 时,在最后一条 user 消息(content 字符串/parts) 追加 "\n\nRespond with a JSON object only."。流式/批量路径都走 buildRequestBody 单点生效。
代理路径(/api/chat openai 分支) 未改(用户当前浏览器直连,代理不再用);后续若用户切回代理需在 server route 也补同样 hint。

### 追加:徽章竞选 AI 返回字段乱（signup/sheriff_candidacy/报名竞选…）真凶 = i18n ICU key 泄漏
根因:zh/en `prompts.badge.signup.task` 模板正文含字面 JSON 示例 `{"signup":true}`;next-intl(ICU) 把 `{` 当占位符解析 → createTranslator 抛错 → translator wrapper catch 返回 key 原文 → 模型 system 里收到的是字面 "prompts.badge.signup.task"（抓包证实），没有输出格式说明 → json_object 下自由发挥字段名，部分上警意图被丢弃(parse 只认 signup)。
修复:
- i18n 模板改叙述式（模板正文禁止字面 JSON 花括号）：prompts.badge.signup.task(zh/en) → “对象只有唯一键 signup,布尔值:报名 true/不报名 false”。
- 另修 customCharacter.import.placeholder(含 {"characters":[…]}) → 叙述式。gameMaster.badgeSignup.systemPrompt/userPrompt 也含字面 JSON(残留未使用,本次未改,若未来启用需同样处理)。
- parse 兜底增强 game-master.ts parseBadgeSignupDecision:多键(signup/sheriff_candidacy/candidacy/run/报名/是否报名…)+任意布尔+中英文意图词,防意图丢失。
- 曾尝试“json_schema 降级时全局注入 schema 文本”,会污染传输 messages 破坏“aiLogger↔传输消息一致”审计,已回退(保留 llm-direct json 关键字兜底 ensure)。
- 顺带验证:translator 对含字面花括号模板抛错返回 key——排查其它 prompt 时可 grep 模板正文 `{` 后跟引号。
- 测试:game-master.badge-signup.test.ts 断言改为叙述式;单人上下文 116/116;tsc 0;lint 干净。徽章 task 现可正确渲染(tsx 验证非 key)。

### 追加:全面排查 i18n 模板字面花括号(同类 ICU key 泄漏)
全量扫描 zh/en messages 所有含 { 的模板并逐段人工确认:
- 已修(裸奔):prompts.badge.signup.task、customCharacter.import.placeholder、gameMaster.badgeSignup.systemPrompt/userPrompt(未引用但同样裸奔,已修成叙述式)。
- 安全(ICU 单引号转义 '{'…'}'):characterGenerator.baseProfilesPrompt / fullPersonasPrompt、gameMaster.dailySummary.systemPrompt(zh/en)——一直正常工作的原因。排查方法备忘:JSON.parse 后扫 `{` 紧跟引号/数字/中文等非合法 ICU 占位开头,并把 `'{...}'` 转义形式排除;看原文确认。
- translator.ts:翻译失败(key 泄漏)日志由「仅 dev console.warn/error」改为「总是 console.error」,生产再遇到同类问题控制台可第一时间看到。
- 规则沉淀:i18n 模板正文禁止出现未转义半角花括号字面;要给模型展示 JSON 示例须用 ICU 单引号转义(如 '{' ... '}')或写成叙述式。
验证:tsc 0;单人上下文 116/116。

### 追加:移动端行动确认按钮被遮挡/不可滚动
现象:矮屏/软键盘顶起时,轮到自己刀人/投票/选警长,选中目标后确认按钮在视口外,页面不能滚。
根因链:
1) page.tsx 游戏根 `h-screen`(100vh) + overflow-hidden;移动浏览器地址栏/键盘压缩可视高度 → 100vh 超出部分被裁,底部天然不可达、页面无法整体滚动。
2) DialogArea `wc-dialog-bottom`(1155) 是 `shrink-0` 且无滚动;行动确认卡(wc-panel 内 action-confirm / witch-poison-confirm,1390+)超长时直接被裁掉。
修复:
- page.tsx 根加 `style={{height:'100dvh'}}`(旧浏览器回落 h-screen),让布局跟随动态视口。
- DialogArea bottom 容器:`shrink-0` 移除 + `min-h-[88px] overflow-y-auto scrollbar-hide`,空间不足时可收缩并内部滚动;新增 actionConfirmRef(useEffect on selectedSeat)在选中目标后对确认卡 `scrollIntoView({behavior:'smooth',block:'nearest'})`,确认按钮自动滚入视野(action-confirm 与 witch-poison-confirm 两卡互斥共用 ref)。
验证:tsc 0;lint 无新增;需真机(移动端)验证视觉效果,沙箱无法预览。

### 追加:右下角控制台按钮改为所有环境可见
DevModeButton/游戏屏/欢迎屏原判定 `NODE_ENV !== "production" && NEXT_PUBLIC_SHOW_DEVTOOLS!=="false"` → 生产静态包/APK 不可见。已放开:仅由 `NEXT_PUBLIC_SHOW_DEVTOOLS` 控制,未设置默认 true。
改动:
- page.tsx 543、WelcomeScreen.tsx 238、DevConsole.tsx DevModeButton:去掉 NODE_ENV 限制(注释说明可用构建变量关闭)。
- DevConsole 悬浮组里绿色「测试复盘报告」按钮仍 dev-only(指向开发路由 /test-analysis),黄色控制台按钮全环境。
- ai-logger.ts canUseStorage():去掉 NODE_ENV!==production → 生产也持久化 AI 日志到 localStorage(DevConsole 打开有数据,上限 800 条);console 打印 shouldPrint 仍限 dev 防刷屏。clearLogsOncePerPageLoad 仅 production 执行(useGameLogic 157)语义未动。
验证:tsc 0;lint 无新增。

### 追加:DevConsole 悬浮按钮改造
- 删除右下角绿色「测试复盘报告」悬浮按钮(dev 也用不到悬浮入口;面板内保留复盘入口 DevConsole.tsx 1218/1229)。
- 控制台按钮(黄色 Wrench)改为可拖动:pointer capture 拖拽,位置 clamp 在视口内,拖动>4px 判定为拖(不触发展开点击),位置持久化到 localStorage `wolfcha_devtools_pos`,欢迎屏/游戏屏共用组件位置一致;默认右下(right/bottom 8px),touch-action:none 保证移动端可拖。
- DevModeButton 顶部 removed useRouter/ChartBar(组件内);文件其它处(1076 面板内)仍用 useRouter/ChartBar 保留 import。
- 类型:文件无 React 默认导出,PointerEvent 用 `import type { PointerEvent as ReactPointerEvent } from "react"`。
验证:tsc 0;lint 无新增(470 行 any 为既有)。

### 追加:首页(WelcomeScreen)顶部操作区调整
- 删除首页右上角 GitHub 按钮(含 stars 拉取逻辑/state)与「加入游戏群」按钮;随之删除已无入口的入群二维码 Dialog、移动端「更多操作」Dialog、handleOpenGroup/groupIcon/discordInviteUrl、useAppLocale、@/components/ui/dialog 导入,isAnyModalOpen 同步收敛。
- 移动端(sm 以下)把原先藏在「···」里的 API 配置与设置改为直接展示:LocaleSwitcher + Key「API」+ GearSix「设置」,与桌面端分支结构一致。新增 i18n key welcome.apiShort(zh/en 均为 "API")。
- 入群二维码仍可从游戏内「设置 → 关于我们」进入,功能未丢失。
验证:JSON 合法;tsc 0;eslint 仅 2 个既有 warning(setDifficulty/isDevModeEnabled)。

### 追加:修复移动端「设置」弹窗无法滚动
- 根因:GameSetupModal 的 DialogContent 只有 `w-[92vw] max-w-md`,没有最大高度与内部滚动;而 ui/dialog.tsx 基础样式是 fixed + top-50% + translate-y-[-50%],内容超出视口后既不能滚也会被裁掉(SettingsModal/ConnectionSettings/CustomCharacterModal 都带 `max-h-[85~88vh] overflow-y-auto`,唯独它漏了)。
- 修复:改为 `w-[92vw] max-w-md max-h-[85vh] overflow-y-auto overscroll-contain`。
- 实测(agent-browser,连的是用户本地 3000 端口的 dev server):390x844 → dialog clientH 713/scrollH 749,overflowY auto,top 63 bottom 781 全在屏内;360x640 → clientH 540/scrollH 749,maxH 544,可滚 209px,滚到底后「背景音量/总开关/角色配音/自动播放对话」全部完整可见。tsc 0。
- 顺带发现:DifficultySelector/PlayerCountSelector 用的 `.wc-difficulty-dialog`(globals.css:822)同样没有 max-height/overflow,但这两个组件已无任何引用(死代码),未处理。

### 追加:修复移动端右下角控制台按钮「消失」(DevModeButton)
- 现象:移动端首页右下角黄色控制台按钮看不见。代码仍在渲染(390x844 实测存在且可见),真因是**坐标越界**:按钮可拖动,坐标存 localStorage `wolfcha_devtools_pos`,但从未做视口校验。历史坐标若来自更大屏幕/横屏/带地址栏窗口(如 x=1200),套用到窄屏后按钮落在屏幕外;移动端没有鼠标,拖不回来 = 看起来消失。
- 次因:`useState(() => readDevButtonPos())` 让服务端(pos=null → right/bottom)与客户端首帧(left/top)产出不同 style,触发 React **hydration 不一致**(dev 下 DevTools 面板 1 Issue)。React 会保留服务端属性,导致 DOM 位置与 React 记录的位置长期不一致,行为不确定。
- 修复(src/components/DevTools/DevConsole.tsx,全部集中在 DevModeButton 区块):
  1. 新增 `clampDevButtonPos()` / `isSamePos()` / `writeDevButtonPos()`,并抽常量 `DEV_BUTTON_SIZE=48`(原拖拽里硬编码 size=48)。
  2. 首帧 state 固定 `null`(与服务端一致,消除 hydration 不一致);新增 `useIsomorphicLayoutEffect`(SSR 用 useEffect,客户端 useLayoutEffect 避免跳一下),挂载后读存档 → 夹回可见范围 → 回写 storage。
  3. 新增 resize/orientationchange 监听重新夹取,覆盖旋转屏幕/地址栏伸缩。
  4. 默认位置改用 `calc(8px + env(safe-area-inset-*))`,避开 iPhone home indicator / Android 手势条。
- 实测(agent-browser,用户本地 3000 端口 dev server):390x844 存档 {1200,900} → 自动夹到 {334,788} 且回写 storage,fullyVisible=true;拖动正常并持久化;1000x900 拖到 y=844 后缩到 500x400 → 自动夹到 y=344,storage 同步更新;清空 storage → 回落默认右下;控制台 hydration 报错消失。
- 验证:tsc 退出码 0;eslint 我改动的 1980 行以后 0 问题(该文件既有 8 errors/12 warnings 的 no-explicit-any 与未使用变量,均为历史遗留,行号 767/1012/1396/1678/1926/1928)。

### 追加:排查移动端底部玩家条横向滚动闪烁(APK 真机)
- 结论:PC 模拟复现不了,因为这是 Android WebView 合成器层面的问题。底部横向滚动条 `.wc-mobile-player-bar__track` 里叠了多种「每帧强制重绘/重采样」的写法,清单:
  1. 每张 ready 卡片带 Tailwind `backdrop-blur-sm`,父级 `.wc-mobile-player-bar` 又带 `backdrop-filter: blur(12px)` → 滚动容器内 backdrop-filter 是已知 WebView 闪烁源(滚动中反复重采样背板)。
  2. `.wc-player-card--selectable { animation: wc-selectable-pulse infinite }` 与 `.wc-player-card--speaking { animation: wc-pulse-border infinite }` 都在动 `box-shadow`/`border-color`(非合成属性)→ 每帧重绘;选人阶段所有可点卡片同时跑。
  3. `.wc-player-card:hover { transform: translateX(4px) }`、`.wc-player-card--selectable:hover { translateX(6px) }` 没有 hover 能力判断 → 触摸设备上手指"点住"会残留 hover,卡片带着 `transition: all 0.3s` 平移/换背景,横向滑动时看着就是卡片在闪。
  4. `.wc-player-card--mobile` 继承 `transition: all 0.3s`,与 framer-motion 逐帧写 transform 打架(浏览器再插值一次)。
  5. `scroll-snap-type: x mandatory` 在 Android WebView 上会在手势/惯性未结束时反复回吸。
- 修复(全部只在移动端生效,桌面视觉不变):
  - globals.css `.wc-mobile-player-bar`:去掉 backdrop-filter,背景改 `var(--glass-bg-strong)`(该条本来就 `md:hidden`,只影响移动端)。
  - globals.css `.wc-mobile-player-bar__track`:`scroll-snap-type: x proximity` + `overscroll-behavior-x: contain`。
  - globals.css:4 条 `:hover` 规则(含 dark 变体与 --selectable)包进 `@media (hover: hover) and (pointer: fine)`;并加 `.wc-player-card--mobile:hover, .wc-player-card--mobile.wc-player-card--selectable:hover { transform: none }`(--selectable 特异度更高必须显式覆盖)。
  - globals.css `.wc-player-card--mobile`:`transition: none`、`backface-visibility: hidden`;新增 `.wc-player-card--mobile.wc-player-card--selectable/.wc-player-card--speaking { animation: none }`(保留静态高亮)。
  - PlayerCardCompact.tsx:`backdrop-blur-sm` 与头像 `group-hover:scale-110`、死亡遮罩 `backdrop-blur-[1px]` 都对 `variant === "mobile"` 关闭(桌面保留)。
- 验证(agent-browser,连用户 3000 端口 dev server):因假网关跑不到游戏台(角色生成失败),改用「在真实页面样式表下注入同 class 探针节点读计算样式」验证,并直接遍历 CSSRuleList 确认 4 条 hover 规则都处于 `(hover: hover) and (pointer: fine)` 内。结果:bar backdrop=none/bg rgba(255,255,255,0.9);track snap=x(即 proximity,Chrome 省略默认值,对照 `x mandatory` 会显示全串)/overscroll-x=contain;mobile card transition=none、backface=hidden、selectable/speaking animationName=none;desktop card transition 仍 `0.3s cubic-bezier`、desktop selectable 仍 `wc-selectable-pulse`(未回归)。tsc 0;eslint 无新增(PlayerCardCompact 既有 6 项问题在 5/39/44/72/144/223 行)。
- 备选下一步(若真机仍闪):`!isReady` 卡片里的 framer-motion 无限 `boxShadow` 内阴影脉冲是 JS 主线程逐帧动画,可对移动端一并关掉。

### 追加:修复移动端发言输入框长文本溢出
- 现象:手打长发言时文字画到输入框外,压住「发送」按钮并溢出屏幕。
- 根因(纯 CSS 布局,已在真实页面注入同 class 探针复现):
  1. TipTap 可编辑区 class 是 `wc-input-field ... max-h-[120px]`,但 `.wc-input-field` 没有 overflow → max-height 只截断元素高度,文字仍 `overflow: visible` 画到框外。
  2. `.wc-input-box { height: 50px }` 是固定高(靠 inline `minHeight:112px` 撑到 112),内容区只有 112-14(top pad)-56(留给发送按钮的 bottom pad)=42px,而可编辑区上限 120px → 即使文字没溢出元素,元素本身也已越出框 22px。
  实测(390x844):box 112px,editable 120px(内容 192px 高)、overflow-y=visible → 文字比框底多出 95px。
- 修复:
  - MentionInput.tsx editorProps class 增加 `overflow-y-auto overscroll-contain break-words leading-6`,并把上限改为 `max-h-[96px] sm:max-h-[120px]`(移动端可视高度更紧)。
  - globals.css `.wc-input-box`:`height: 50px` → `min-height: 50px`,让框跟着多行文本长高(上限 14+120+56≈190px,移动端 14+96+56≈166px)。
  - globals.css 给 `.wc-input-field` 加细滚动条(`scrollbar-width: thin` + `::-webkit-scrollbar{width:6px}`,金色半透明 thumb)。
- 实测修复后:390x844 → box 168px、editable 96px/scrollH 192 → scrollable=true、editableInsideBox=true、boxFullyOnScreen=true;模拟在末尾继续输入 → scrollTop 自动到 119(max 120)且光标可见(caretVisible=true);1280x900 → box 192px、editable 120px、仍在框内且在屏内。
- 验证:tsc 0;eslint MentionInput 仅 1 项既有 `no-explicit-any`(334 行 `(e as any).isComposing`,非本次改动)。

### 追加:README 增加二次开发声明 + 更正许可证标注
- 用户诉求:准备推自己的 GitHub 仓库,想在每个 README 里说明本项目基于 https://github.com/oil-oil/wolfcha 二次开发(他以为上游是 MIT)。
- **核实结果:上游不是 MIT,是 Apache-2.0**。证据:GitHub API `/repos/oil-oil/wolfcha` → `spdx_id: apache-2.0`;仓库 LICENSE 是 Apache-2.0 全文(202 行);上游无 NOTICE(API contents/NOTICE → 404)。三个 README 里写的 "MIT" 是上游文档错误(全仓库仅这 3 处提到 MIT,package.json 无 license 字段)。
- 改动:README.md / README.zh.md / README.en.md 各加一段「二次开发说明」(en: Derivative work),并把 License 段从 `MIT` 改为 `Apache License 2.0` + 说明「上游同为 Apache-2.0、本仓库为二次开发沿用同一许可证、保留原版权声明」。README.en.md 保留其 emoji 标题风格(🔗 / 📄)。
- 给用户的合规结论:必须保留 Apache-2.0(不能改标 MIT、不能删版权声明),可为自己新增部分加版权声明;§4(b) 要求声明修改过文件,§4(d) 仅在**上游有 NOTICE 时**才强制附带(此处无)。
- 仍未做(已在回复中提示):README.md 的 `git clone` 仍指向上游;本地 `origin` 仍指向上游,推自己仓库前需改 remote;package.json 可补 `"license": "Apache-2.0"`;可选加 NOTICE 文件。
- 已把许可证事实与仓库 remote 情况写入 .workbuddy/memory/MEMORY.md(新建)。
