# Wolfcha 纯前端版（静态导出 / APK）
#
# 本项目是纯前端应用，不再依赖任何服务端环境变量。
# AI 调用所需的 OpenAI 兼容网关地址、API Key、模型名，以及可选的
# MiniMax TTS Key / Group ID，均由玩家在应用内「模型/语音连接」设置中
# 填写并保存在本机 localStorage，无需在构建时配置。
#
# 如需指定前端展示的构建版本，可以设置：
# NEXT_PUBLIC_APP_VERSION=1.0.0
